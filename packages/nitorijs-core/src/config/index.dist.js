module.exports = {
  discordToken: '<DISCORD_TOKEN>',
  prefix: '/',
  add: async Nitori => {
    try {
      // Add plugins here
      // Nitori.set(plugin.info, plugin.run)
    } catch (err) {
      console.error(err);
    }
  }
};
